---
pkg::collectd: 5.12.0-24.el%{facts.os.release.major}
collectd::install::packages:
  collectd-disk:
    ensure: "%{lookup('pkg::collectd')}"
  collectd-python:
    ensure: "%{lookup('pkg::collectd')}"
